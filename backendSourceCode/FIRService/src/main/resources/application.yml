server:
  port: 8086
spring:
  application:
    name: FIR-MANAGEMENT-SERVICE
  data:
    mongodb:
      uri: mongodb://******
      database: ******

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://*****